<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.voodoowarez.cordova.networkdisco"
	android:versionCode="1"
	android:versionName="1.0-SNAPSHOT" >

	<!-- NetworkDisco is a plugin, not a Android project, and thus has no manifest, but we're seeking -->
	<!-- to make the Maven tooling happy enough to link our dependencies in the IDE -->

	<uses-sdk
		android:minSdkVersion="16"
		android:targetSdkVersion="16" />

	<application
		android:allowBackup="true"
		android:icon="@drawable/ic_launcher"
		android:label="@string/app_name"
		android:theme="@style/AppTheme">
<!--
		<activity android:name=".HelloAndroidActivity" >
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
-->
	</application>
</manifest>
